'use strict'

/**
* This is the player class that acts as a wrapper for slack user
*/
class Player {
  constructor(symbol, username) {
    this.symbol = symbol;
    this.username = username;
  }
}

module.exports = Player;
